# 🚀 RestoAnalytic

Аналитическая платформа для ресторанного бизнеса.

## 📥 Установка и Запуск

### 1. Подготовка окружения
Убедитесь, что установлен Python 3.9+.

```bash
# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Mac/Linux
# venv\Scripts\activate   # Windows

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка секретов
Создайте файл `.streamlit/secrets.toml` (если не создан):

```toml
YANDEX_TOKEN = "ваш_токен"
# поддерживаются оба варианта ключей:
TG_BOT_TOKEN = "ваш_токен_бота"
TG_CHAT_ID = "ваш_chat_id"
# или:
TELEGRAM_TOKEN = "ваш_токен_бота"
TELEGRAM_CHAT_ID = "ваш_chat_id"
```

### 3. Запуск Приложения
```bash
streamlit run app.py
```

## 🔄 Синхронизация Данных
Для обновления данных из Яндекс.Диска запустите:
```bash
python sync_data.py
```
Это скачает новые отчеты и обновит кеш `data_cache.parquet`.

## 🤖 Telegram Бот
Для отправки ежедневного отчета (можно настроить через cron):
```bash
python daily_report.py
```

## 🛠 Структура Проекта
- `app.py` - Основное приложение (Streamlit Dashboard).
- `data_engine.py` - Ядро обработки данных (парсинг, очистка, аналитика).
- `sync_data.py` - Скрипт синхронизации с облаком.
- `daily_report.py` - Скрипт отправки отчетов в Telegram.
- `telegram_utils.py` - Утилиты для форматирования сообщений.

## 🐞 Отладка
Если данные сходятся не полностью (себестоимость отличается от 1С):
1. Откройте приложение.
2. В боковом меню раскройте **"🐞 Debug: Отброшенные строки"**.
3. Проверьте список исключенных позиций.

---
Разработано с ❤️ для Бар МЕСТО.
